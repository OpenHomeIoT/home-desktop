import assert from "assert";

import MockDeviceDatabase from "../mocks/db/MockDeviceDatabase";

// describe("DeviceDatabase Tests", () => {
//   it("open(): Should be able to open without fail.", (done) => {
//     MockDeviceDatabase.getInstance().open().then(done).catch(done);
//   });

//   it("close(): Should be able to close without fail.", () => {
//     MockDeviceDatabase.getInstance().close();
//   });
// });